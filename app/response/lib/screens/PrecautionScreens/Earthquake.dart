import 'package:flutter/material.dart';

class Earthquake extends StatelessWidget {
  static const String id = '/Earthquake';

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
